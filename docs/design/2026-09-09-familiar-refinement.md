# Доработка выбранного макета Dockge2

- [x] Согласовать первый показ интерфейса с готовностью сессии и данных.
- [x] Обновить знак, типографику, цветные инициалы и нижнее действие.
- [x] Подключить выбор сервера к существующим агентам.
- [x] Выделить создание стека в страницу с Git и Compose, сохранить docker run.
- [x] Реализовать получение Git, сравнение и явный выбор файлов.
- [x] Проверить браузерные переходы и адаптивность.
- [x] Проверить тесты, типы, сборку и актуализировать документацию.

## Проверки

- `npm run check`: 257 тестов, lint и TypeScript прошли. Покрытие строк 81,81%, ветвей 84,65%, функций 86,27%; пороги не менялись.
- `npm run build:frontend` и `git diff --check` прошли.
- Браузер: вход монтирует рабочую область один раз сразу со списком; переподключение сохраняет DOM. Проверены светлая/темная темы, экраны 390 px и настольный, переходы создания, конвертация docker run, отчет и возврат команды.
- Настоящий временный Git-репозиторий: клонирование по HTTP, fetch нового коммита, две стороны сравнения, выбор Compose из Git и локального README, сохранение без запуска. Байты комментария Compose и локальные заметки сохранены; HEAD обновлен, README остается измененным. При перезапуске сервера старое сравнение отклонено без записи.
- Выбор всех серверов и текущего проверен в интерфейсе. Второго физического сервера в проверке не было.
- Полный набор Docker/E2E не запускался. E2E создания обновлены под новую страницу. Пользовательские контейнеры не запускались, не останавливались и не пересоздавались.

Изменения находятся в рабочей копии вместе с предыдущей реализацией тем, доступа и стабильности. Коммит, публикация и обновление рабочего сервера не выполнялись. Временный Git-репозиторий и тестовые данные находятся вне проекта в /tmp.
