# A version younger than a week is not chosen by install or update: most hijacked
# releases are found and pulled within days. npm 11.10 and later obey it; `npm ci`
# installs the lockfile as it is
min-release-age=7
