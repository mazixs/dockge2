{
    "description": "The entrypoint becomes a one-element list, so it runs as one executable like with docker run, and the arguments after the image become the command, a list split the way the shell split it.",
    "command": "docker run -d --entrypoint /bin/sh alpine -c 'echo hello && sleep 3600'",
    "verdict": "kept",
    "compose": {
        "topLevel": [
            "services"
        ],
        "service": "alpine",
        "keys": [
            "image",
            "entrypoint",
            "command"
        ],
        "values": {
            "image": "alpine",
            "entrypoint": [
                "/bin/sh"
            ],
            "command": [
                "-c",
                "echo hello && sleep 3600"
            ]
        }
    },
    "report": {
        "carried": [
            "-d",
            "--entrypoint"
        ],
        "review": [],
        "dropped": []
    },
    "composeConfig": {
        "valid": true
    }
}
