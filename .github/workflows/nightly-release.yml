name: Nightly release retired
on:
  workflow_dispatch:
permissions: {}
jobs:
  instructions:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Use a vX.Y.Z-rc.N tag and the Release workflow."
          echo "Every published update must pass the same artifact and upgrade gates."
          exit 1
